apiVersion: apps/v1
kind: Deployment
metadata:
  name: spark-master
  namespace: bigdata
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spark-master
  template:
    metadata:
      labels:
        app: spark-master
    spec:
      containers:
      - name: spark-master
        image: mquan1503/bigdata-spark:latest  # <--- Switched to Official Image
        imagePullPolicy: IfNotPresent
        command: [
          "/opt/spark/bin/spark-class", 
          "org.apache.spark.deploy.master.Master"
        ]
        env:
        - name: SPARK_MASTER_OPTS
          value: "-Dspark.master.rest.enabled=true -Dspark.master.rest.port=6066"
        - name: SPARK_DAEMON_MEMORY
          value: "512m"
        ports:
        - containerPort: 7077
        - containerPort: 6066
        - containerPort: 8080
        resources:
          requests:
            cpu: "250m"
            memory: "512Mi"
          limits:
            cpu: "1"
            memory: "1Gi"
        volumeMounts:
        - name: ivy-cache
          mountPath: /tmp
      volumes:
      - name: ivy-cache
        emptyDir: {}
---
# 3. Spark Worker Deployment (UPDATED for Apache Image)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: spark-worker
  namespace: bigdata
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spark-worker
  template:
    metadata:
      labels:
        app: spark-worker
    spec:
      containers:
      - name: spark-worker
        image: mquan1503/bigdata-spark:latest # <--- Switched to Official Image
        imagePullPolicy: IfNotPresent
        command: [
          "/opt/spark/bin/spark-class", 
          "org.apache.spark.deploy.worker.Worker",
          "spark://spark-master-svc:7077"
        ]
        env:
        - name: SPARK_WORKER_CORES
          value: "3"
        - name: SPARK_WORKER_MEMORY
          value: "5g"
        - name: SPARK_DAEMON_MEMORY
          value: "512m"
        resources:
          requests:
            cpu: "1"
            memory: "3Gi"
          limits:
            cpu: "3"
            memory: "6Gi"
        volumeMounts:
        - name: spark-work-dir 
          mountPath: /opt/spark/work
        - name: ivy-cache
          mountPath: /tmp
      volumes:
      - name: spark-work-dir 
        emptyDir: {}
      - name: ivy-cache
        emptyDir: {}
      
